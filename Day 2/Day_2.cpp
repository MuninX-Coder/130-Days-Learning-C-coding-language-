#include <iostream>  // This is a precrocessor directive, this (#include iostream ) means hey c++ include that iostream library, so that we can use its functions of cin and cout
using namespace std;  //  it lets us use shortcuts like cout instead of std::cout.


int main(){        // every c++ programme startes with the main function, inside the curly brasis {} every code will execute line by line
    cout << "structure of c++ programmer" << endl;    // cout is a funtions of iostream library, cout is used to disply output in the screen.endl means end line (like pressing "Enter").
    return 0;  // return 0 says everythings runs smothly now return intergers 0.
}